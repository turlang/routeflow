PORT=3001
DATABASE_URL="postgresql://routeflow:routeflow@localhost:5432/routeflow?schema=public"
JWT_SECRET="troque-por-um-segredo-longo-e-aleatorio"
CORS_ORIGIN="http://localhost:5173,http://localhost:5500,http://127.0.0.1:5500,https://turlang.github.io"

# Administrative bootstrap. Comma-separated account emails. A matching authenticated account is promoted to ADMIN on login.
# Never expose this value in frontend code.
ADMIN_EMAILS=""

# Routing. Public OSRM is development-only; configure a contracted/self-hosted endpoint for production.
ROUTING_BASE_URL="https://router.project-osrm.org"
ROUTING_TIMEOUT_MS=12000

# Object storage for private Proof of Delivery photos. Leave blank until provider credentials exist.
STORAGE_PROVIDER=""
STORAGE_ENDPOINT=""
STORAGE_BUCKET=""
STORAGE_REGION=""
STORAGE_ACCESS_KEY_ID=""
STORAGE_SECRET_ACCESS_KEY=""

# Recurring billing adapter. Live checkout/webhooks remain disabled without provider credentials.
BILLING_PROVIDER=""
BILLING_WEBHOOK_SECRET=""
BILLING_API_KEY=""
