import test from'node:test';import assert from'node:assert/strict';import{requestContext,errorRecord}from'../src/observability.js';
test('request context preserves safe caller request id',()=>{let finish;const req={headers:{'x-request-id':'trace-123'},method:'GET',originalUrl:'/v1/routes?x=1'},res={statusCode:200,headers:{},setHeader(k,v){this.headers[k]=v},on(name,fn){if(name==='finish')finish=fn}};requestContext(req,res,()=>{});assert.equal(req.requestId,'trace-123');assert.equal(res.headers['X-Request-Id'],'trace-123');assert.equal(typeof finish,'function')});
test('error record omits query string and carries request id',()=>{const parsed=JSON.parse(errorRecord(new Error('boom'),{requestId:'abc',method:'POST',originalUrl:'/v1/test?token=secret'}));assert.equal(parsed.requestId,'abc');assert.equal(parsed.path,'/v1/test');assert.equal(parsed.message,'boom')});
