export function routingCoordinates(value,{max=250}={}){if(!Array.isArray(value)||value.length<2||value.length>max)throw new Error(`Informe entre 2 e ${max} coordenadas.`);return value.map((p,i)=>{const lat=Number(p?.lat),lon=Number(p?.lon);if(!Number.isFinite(lat)||lat<-90||lat>90||!Number.isFinite(lon)||lon<-180||lon>180)throw new Error(`Coordenada ${i+1} inválida.`);return{lat,lon}})}export const osrmCoordinates=points=>points.map(p=>`${p.lon},${p.lat}`).join(';');
