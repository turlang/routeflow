const base=(process.env.ROUTING_BASE_URL||'https://router.project-osrm.org').replace(/\/$/,'');const timeoutMs=Math.max(1000,Number(process.env.ROUTING_TIMEOUT_MS||12000));export const routingConfigured=()=>Boolean(process.env.ROUTING_BASE_URL)&&!base.includes('router.project-osrm.org');export async function routingFetch(path){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{const r=await fetch(`${base}${path}`,{signal:controller.signal,headers:{accept:'application/json','user-agent':'RouteFlow/1.0'}});if(!r.ok)throw new Error(`Routing HTTP ${r.status}`);const data=await r.json();if(data.code&&data.code!=='Ok')throw new Error(`Routing ${data.code}`);return data}finally{clearTimeout(timer)}}
