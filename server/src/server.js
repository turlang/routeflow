import 'dotenv/config';
import express from 'express';
import cors from 'cors';
import helmet from'helmet';
import bcrypt from'bcryptjs';
import jwt from'jsonwebtoken';
import{PrismaClient}from'@prisma/client';
import{z}from'zod';
const app=express(),db=new PrismaClient(),PORT=Number(process.env.PORT||3001),SECRET=process.env.JWT_SECRET;if(!SECRET||SECRET.length<24)throw new Error('JWT_SECRET deve ter pelo menos 24 caracteres.');const origins=(process.env.CORS_ORIGIN||'').split(',').map(v=>v.trim()).filter(Boolean);app.use(helmet());app.use(cors({origin:(o,cb)=>!o||origins.includes(o)?cb(null,true):cb(new Error('Origem não autorizada'))}));app.use(express.json({limit:'1mb'}));const normalizeAddress=s=>String(s||'').trim().toLocaleLowerCase('pt-BR').replace(/\s+/g,' '),tokenFor=u=>jwt.sign({sub:u.id,email:u.email},SECRET,{expiresIn:'30d',issuer:'routeflow-api',audience:'routeflow-web'});function auth(req,res,next){const raw=req.headers.authorization?.replace(/^Bearer\s+/i,'');try{req.auth=jwt.verify(raw,SECRET,{issuer:'routeflow-api',audience:'routeflow-web'});next()}catch{return res.status(401).json({error:'Não autenticado'})}}const credentials=z.object({email:z.string().email().transform(v=>v.toLowerCase().trim()),password:z.string().min(8).max(100),name:z.string().trim().max(80).optional()}),deliveryInput=z.object({clientId:z.string().max(160).optional(),routeId:z.string().optional(),addressId:z.string().optional(),address:z.string().min(3).max(500).optional(),packageNo:z.string().max(100).optional(),size:z.enum(['Pequeno','Médio','Grande']).optional(),status:z.enum(['PENDING','DELIVERED','FAILED']).default('PENDING'),deliveredAt:z.string().datetime().optional(),latitude:z.number().optional(),longitude:z.number().optional(),accuracy:z.number().nonnegative().optional(),notes:z.string().max(2000).optional()}),routeInput=z.object({sourceFilename:z.string().max(255).optional(),startedAt:z.string().datetime().optional(),plannedKm:z.number().nonnegative().optional(),plannedMinutes:z.number().int().nonnegative().optional()});
async function ownedRelations(userId,{addressId,routeId}){if(addressId&&!await db.address.findFirst({where:{id:addressId,userId},select:{id:true}}))return{error:'Endereço não pertence à conta'};if(routeId&&!await db.route.findFirst({where:{id:routeId,userId},select:{id:true}}))return{error:'Rota não pertence à conta'};return{}}
app.get('/health',(_,res)=>res.json({ok:true,service:'routeflow-api'}));app.post('/v1/auth/register',async(req,res)=>{const p=credentials.safeParse(req.body);if(!p.success)return res.status(400).json({error:'Dados inválidos'});if(await db.user.findUnique({where:{email:p.data.email}}))return res.status(409).json({error:'E-mail já cadastrado'});const user=await db.user.create({data:{email:p.data.email,name:p.data.name,passwordHash:await bcrypt.hash(p.data.password,12)},select:{id:true,email:true,name:true}});res.status(201).json({user,token:tokenFor(user)})});app.post('/v1/auth/login',async(req,res)=>{const p=credentials.pick({email:true,password:true}).safeParse(req.body);if(!p.success)return res.status(400).json({error:'Dados inválidos'});const found=await db.user.findUnique({where:{email:p.data.email}});if(!found||!await bcrypt.compare(p.data.password,found.passwordHash))return res.status(401).json({error:'E-mail ou senha inválidos'});const user={id:found.id,email:found.email,name:found.name};res.json({user,token:tokenFor(user)})});app.get('/v1/me',auth,async(req,res)=>res.json(await db.user.findUnique({where:{id:req.auth.sub},select:{id:true,email:true,name:true,createdAt:true}})));
app.get('/v1/addresses',auth,async(req,res)=>res.json(await db.address.findMany({where:{userId:req.auth.sub},orderBy:{updatedAt:'desc'}})));app.post('/v1/addresses/import',auth,async(req,res)=>{const input=z.object({addresses:z.array(z.object({address:z.string().min(3),latitude:z.number().nullable().optional(),longitude:z.number().nullable().optional()})).max(2000)}).safeParse(req.body);if(!input.success)return res.status(400).json({error:'Endereços inválidos'});let created=0,updated=0;for(const a of input.data.addresses){const normalizedKey=normalizeAddress(a.address),existing=await db.address.findUnique({where:{userId_normalizedKey:{userId:req.auth.sub,normalizedKey}}});if(existing){await db.address.update({where:{id:existing.id},data:{address:a.address,latitude:a.latitude??existing.latitude,longitude:a.longitude??existing.longitude,lastImportedAt:new Date(),importCount:{increment:1}}});updated++}else{await db.address.create({data:{userId:req.auth.sub,normalizedKey,address:a.address,latitude:a.latitude,longitude:a.longitude,type:'Casa'}});created++}}res.json({created,updated,total:input.data.addresses.length})});app.patch('/v1/addresses/:id',auth,async(req,res)=>{const body=z.object({type:z.enum(['Casa','Comércio','Trabalho','Prédio sem porteiro','Prédio com porteiro']).optional(),hours:z.string().max(100).nullable().optional(),access:z.string().max(1000).nullable().optional(),parking:z.string().max(1000).nullable().optional(),notes:z.string().max(2000).nullable().optional()}).safeParse(req.body);if(!body.success)return res.status(400).json({error:'Dados inválidos'});const current=await db.address.findFirst({where:{id:req.params.id,userId:req.auth.sub}});if(!current)return res.status(404).json({error:'Endereço não encontrado'});res.json(await db.address.update({where:{id:current.id},data:body.data}))});
app.get('/v1/deliveries',auth,async(req,res)=>res.json(await db.delivery.findMany({where:{userId:req.auth.sub},include:{address:true},orderBy:{deliveredAt:'desc'},take:1000})));app.post('/v1/deliveries',auth,async(req,res)=>{const body=deliveryInput.safeParse(req.body);if(!body.success)return res.status(400).json({error:'Dados inválidos'});const relation=await ownedRelations(req.auth.sub,body.data);if(relation.error)return res.status(403).json(relation);let addressId=body.data.addressId;if(!addressId&&body.data.address){const normalizedKey=normalizeAddress(body.data.address),address=await db.address.upsert({where:{userId_normalizedKey:{userId:req.auth.sub,normalizedKey}},update:{address:body.data.address},create:{userId:req.auth.sub,normalizedKey,address:body.data.address,type:'Casa'}});addressId=address.id}const{address,...data}=body.data;res.status(201).json(await db.delivery.create({data:{...data,addressId,userId:req.auth.sub,deliveredAt:data.deliveredAt?new Date(data.deliveredAt):undefined}}))});app.post('/v1/deliveries/import',auth,async(req,res)=>{const parsed=z.object({deliveries:z.array(deliveryInput.extend({clientId:z.string().min(1).max(160)})).max(1000)}).safeParse(req.body);if(!parsed.success)return res.status(400).json({error:'Histórico inválido'});let created=0,existing=0;for(const item of parsed.data.deliveries){const duplicate=await db.delivery.findUnique({where:{userId_clientId:{userId:req.auth.sub,clientId:item.clientId}}});if(duplicate){existing++;continue}const relation=await ownedRelations(req.auth.sub,item);if(relation.error)return res.status(403).json(relation);let addressId=item.addressId;if(!addressId&&item.address){const normalizedKey=normalizeAddress(item.address),address=await db.address.upsert({where:{userId_normalizedKey:{userId:req.auth.sub,normalizedKey}},update:{address:item.address},create:{userId:req.auth.sub,normalizedKey,address:item.address,type:'Casa'}});addressId=address.id}const{address,...data}=item;await db.delivery.create({data:{...data,addressId,userId:req.auth.sub,deliveredAt:data.deliveredAt?new Date(data.deliveredAt):undefined}});created++}res.json({created,existing,total:parsed.data.deliveries.length})});
app.get('/v1/routes',auth,async(req,res)=>res.json(await db.route.findMany({where:{userId:req.auth.sub},include:{_count:{select:{deliveries:true}}},orderBy:{createdAt:'desc'},take:200})));app.post('/v1/routes',auth,async(req,res)=>{const p=routeInput.safeParse(req.body);if(!p.success)return res.status(400).json({error:'Rota inválida'});res.status(201).json(await db.route.create({data:{userId:req.auth.sub,sourceFilename:p.data.sourceFilename,status:'ACTIVE',startedAt:p.data.startedAt?new Date(p.data.startedAt):new Date(),plannedKm:p.data.plannedKm,plannedMinutes:p.data.plannedMinutes}}))});app.patch('/v1/routes/:id',auth,async(req,res)=>{const p=z.object({status:z.enum(['PLANNED','ACTIVE','COMPLETED','CANCELLED']).optional(),finishedAt:z.string().datetime().optional()}).safeParse(req.body);if(!p.success)return res.status(400).json({error:'Dados da rota inválidos'});const route=await db.route.findFirst({where:{id:req.params.id,userId:req.auth.sub}});if(!route)return res.status(404).json({error:'Rota não encontrada'});res.json(await db.route.update({where:{id:route.id},data:{status:p.data.status,finishedAt:p.data.finishedAt?new Date(p.data.finishedAt):undefined}}))});app.use((err,req,res,next)=>{console.error(err);res.status(500).json({error:'Erro interno'})});app.listen(PORT,()=>console.log(`RouteFlow API em :${PORT}`));
