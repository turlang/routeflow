export function deliveryMetrics(deliveries=[]){const total=deliveries.length,delivered=deliveries.filter(x=>x.status==='DELIVERED').length,failed=deliveries.filter(x=>x.status==='FAILED').length,pending=total-delivered-failed;return{total,delivered,failed,pending,successRate:total?Number((delivered/total*100).toFixed(1)):0}}
export function routeMetrics(routes=[]){const total=routes.length,completed=routes.filter(x=>x.status==='COMPLETED').length,active=routes.filter(x=>x.status==='ACTIVE').length,cancelled=routes.filter(x=>x.status==='CANCELLED').length,km=routes.reduce((n,x)=>n+Number(x.plannedKm||0),0);return{total,completed,active,cancelled,plannedKm:Number(km.toFixed(1))}}
