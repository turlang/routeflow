const provider=()=>String(process.env.STORAGE_PROVIDER||'').trim().toLowerCase();export function storageConfigured(){return Boolean(provider()&&process.env.STORAGE_ENDPOINT&&process.env.STORAGE_BUCKET&&process.env.STORAGE_ACCESS_KEY_ID&&process.env.STORAGE_SECRET_ACCESS_KEY)}export function storageStatus(){return{configured:storageConfigured(),provider:provider()||null,bucket:storageConfigured()?process.env.STORAGE_BUCKET:null}}export function proofObjectKey({userId,deliveryId,extension='jpg'}){const clean=v=>String(v||'').replace(/[^a-zA-Z0-9_-]/g,'').slice(0,100),ext=String(extension||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'').slice(0,5)||'jpg';if(!clean(userId)||!clean(deliveryId))throw new Error('Identidade do comprovante inválida');return`proofs/${clean(userId)}/${clean(deliveryId)}.${ext}`}
