const ACTIVE=new Set(['ACTIVE','TRIALING']);
export const billingConfigured=()=>Boolean(process.env.BILLING_PROVIDER&&process.env.BILLING_WEBHOOK_SECRET);
export function normalizeSubscriptionEvent(input={}){const customerId=String(input.customerId||input.customer_id||'').trim(),status=String(input.status||'').trim().toUpperCase(),plan=String(input.plan||'FREE').trim().toUpperCase(),periodEnd=input.periodEnd||input.period_end||null;if(!customerId)throw new Error('billing customerId ausente');if(!['FREE','DRIVER','PRO','TEAM','BUSINESS'].includes(plan))throw new Error('billing plan inválido');if(!['INACTIVE','TRIALING','ACTIVE','PAST_DUE','CANCELLED'].includes(status))throw new Error('billing status inválido');const billingPeriodEnd=periodEnd?new Date(periodEnd):null;if(billingPeriodEnd&&Number.isNaN(billingPeriodEnd.getTime()))throw new Error('billing periodEnd inválido');return{customerId,status,plan:ACTIVE.has(status)?plan:'FREE',billingPeriodEnd}}
export function validBillingSecret(value){const expected=process.env.BILLING_WEBHOOK_SECRET||'';return Boolean(expected&&value&&String(value)===expected)}
