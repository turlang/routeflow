import{hasSession,me,listRoutes}from'./api.js';
const PLAN_LABELS={FREE:'Grátis',DRIVER:'Motorista',PRO:'Pro',TEAM:'Equipe',BUSINESS:'Business'};
const STATUS_LABELS={ACTIVE:'Ativa',INACTIVE:'Inativa',CANCELLED:'Cancelada',PAST_DUE:'Pagamento pendente',TRIALING:'Período de teste'};
const PLANS=[
 {id:'FREE',price:'R$ 0',tag:'Começar',desc:'Para conhecer o RouteFlow.',features:['2 rotas por mês','Até 25 paradas por rota','1 motorista']},
 {id:'DRIVER',price:'R$ 19,90/mês',tag:'Motorista',desc:'Para quem faz entregas todos os dias.',features:['30 rotas por mês','Até 80 paradas por rota','Navegação e sincronização','Histórico de 90 dias','Comprovante de entrega']},
 {id:'PRO',price:'R$ 39,90/mês',tag:'Pro',desc:'Mais capacidade e histórico completo.',features:['Até 200 paradas por rota','Histórico completo','Exportação','Inteligência de endereços','Comprovantes e ocorrências']},
 {id:'TEAM',price:'R$ 99,90/mês',tag:'Equipe',desc:'Operação com vários motoristas.',features:['Até 5 motoristas','Painel consolidado','Histórico da equipe','Indicadores e ocorrências']},
 {id:'BUSINESS',price:'A partir de R$ 249,90/mês',tag:'Business',desc:'Para operações maiores e personalizadas.',features:['Múltiplos usuários e gestores','Limites negociados','Relatórios e integrações','Onboarding e suporte']}
];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const date=v=>v?new Intl.DateTimeFormat('pt-BR',{dateStyle:'medium'}).format(new Date(v)):'Não definida';
function shell(){let modal=document.getElementById('subscriberPortal');if(modal)return modal;modal=document.createElement('section');modal.id='subscriberPortal';modal.className='subscriber-modal';modal.hidden=true;modal.innerHTML=`<div class="subscriber-card"><div class="subscriber-head"><div><small>ÁREA DO ASSINANTE</small><h2 id="subscriberTitle">Minha conta</h2></div><button id="subscriberClose" class="secondary" type="button">Fechar</button></div><div id="subscriberBody" class="subscriber-body"><p>Carregando sua conta…</p></div></div>`;document.body.appendChild(modal);modal.addEventListener('click',e=>{if(e.target===modal)close()});modal.querySelector('#subscriberClose').onclick=close;return modal}
function close(){const el=document.getElementById('subscriberPortal');if(el)el.hidden=true}
async function usage(){try{const routes=await listRoutes(),now=new Date(),start=new Date(now.getFullYear(),now.getMonth(),1);return routes.filter(r=>new Date(r.createdAt)>=start).length}catch{return null}}
function showPlans(currentPlan='FREE'){const modal=shell(),body=modal.querySelector('#subscriberBody');modal.querySelector('#subscriberTitle').textContent='Planos RouteFlow';body.innerHTML=`<button id="plansBack" class="secondary subscriber-back" type="button">← Minha conta</button><div class="plan-picker-intro"><h3>Escolha o plano para sua operação</h3><p>Compare os recursos disponíveis. A contratação online será liberada quando o provedor de pagamentos estiver conectado.</p></div><div class="plan-picker">${PLANS.map(p=>`<article class="plan-option ${p.id===currentPlan?'current':''}"><div><small>${esc(p.tag.toUpperCase())}</small><h3>${esc(PLAN_LABELS[p.id])}</h3><strong>${esc(p.price)}</strong><p>${esc(p.desc)}</p></div><ul>${p.features.map(f=>`<li>✓ ${esc(f)}</li>`).join('')}</ul>${p.id===currentPlan?'<button type="button" disabled>Plano atual</button>':`<button type="button" class="secondary plan-interest" data-plan="${p.id}">Escolher ${esc(PLAN_LABELS[p.id])}</button>`}</article>`).join('')}</div><div id="planMessage" class="plan-message">Nenhuma cobrança será feita enquanto o pagamento online não estiver ativado.</div>`;body.querySelector('#plansBack').onclick=render;body.querySelectorAll('.plan-interest').forEach(btn=>btn.onclick=()=>{const plan=PLAN_LABELS[btn.dataset.plan]||btn.dataset.plan;body.querySelector('#planMessage').innerHTML=`<strong>${esc(plan)} selecionado.</strong> O plano está disponível para comparação, mas a contratação ainda aguarda a integração do provedor de pagamentos. Nenhuma cobrança foi realizada.`;body.querySelector('#planMessage').scrollIntoView({behavior:'smooth',block:'nearest'})})}
async function render(){const modal=shell(),body=modal.querySelector('#subscriberBody');modal.querySelector('#subscriberTitle').textContent='Minha conta';modal.hidden=false;if(!hasSession()){body.innerHTML='<div class="subscriber-empty"><h3>Entre na sua conta</h3><p>A Área do Assinante fica disponível depois do login.</p></div>';return}body.innerHTML='<p>Carregando sua assinatura…</p>';try{const[user,routesUsed]=await Promise.all([me(),usage()]),e=user.entitlements||{},plan=user.plan||'FREE',status=user.subscriptionStatus||'INACTIVE',limit=e.routesPerMonth??e.monthlyRoutes??null,percent=limit&&routesUsed!==null?Math.min(100,Math.round(routesUsed/limit*100)):0;body.innerHTML=`<section class="subscriber-profile"><div class="subscriber-avatar">${esc((user.name||user.email||'R').slice(0,1).toUpperCase())}</div><div><strong>${esc(user.name||'Assinante RouteFlow')}</strong><span>${esc(user.email)}</span><small>Cliente desde ${date(user.createdAt)}</small></div></section><div class="subscriber-grid"><article class="subscriber-plan"><small>PLANO ATUAL</small><h3>${esc(PLAN_LABELS[plan]||plan)}</h3><b>${esc(PLANS.find(p=>p.id===plan)?.price||'Plano personalizado')}</b><span class="subscriber-status ${status==='ACTIVE'?'active':''}">${esc(STATUS_LABELS[status]||status)}</span></article><article><small>RENOVAÇÃO / PERÍODO</small><h3>${date(user.billingPeriodEnd)}</h3><p>${plan==='FREE'?'O plano gratuito não possui cobrança recorrente.':'A data é sincronizada pelo provedor de cobrança.'}</p></article><article class="subscriber-usage"><small>USO NESTE MÊS</small><h3>${routesUsed===null?'—':routesUsed}${limit?` / ${limit}`:''} rotas</h3>${limit?`<div class="usage-track"><i style="width:${percent}%"></i></div><p>${Math.max(0,limit-(routesUsed||0))} rota(s) disponíveis no período.</p>`:'<p>Limite mensal conforme contrato.</p>'}</article><article><small>LIMITE POR ROTA</small><h3>${e.stopsPerRoute??e.maxStops??'—'} paradas</h3><p>Os limites são aplicados pelo servidor à criação de novas rotas.</p></article></div><section class="subscriber-section"><div><h3>Assinatura e cobrança</h3><p>Compare os planos agora. Pagamento, cartão e faturas serão habilitados quando o provedor de cobrança estiver conectado.</p></div><div class="subscriber-actions"><button type="button" id="subscriberPlans">Ver planos</button><button type="button" class="secondary" disabled>Forma de pagamento</button><button type="button" class="secondary" disabled>Faturas</button></div></section><section class="subscriber-section"><div><h3>Conta</h3><p>Seus dados de acesso e sincronização ficam vinculados a esta conta RouteFlow.</p></div><dl class="subscriber-details"><div><dt>Nome</dt><dd>${esc(user.name||'Não informado')}</dd></div><div><dt>E-mail</dt><dd>${esc(user.email)}</dd></div><div><dt>Status</dt><dd>${esc(STATUS_LABELS[status]||status)}</dd></div></dl></section><p class="subscriber-note">Nenhuma cobrança é realizada por esta tela enquanto o provedor financeiro não estiver ativado.</p>`;body.querySelector('#subscriberPlans').onclick=()=>showPlans(plan)}catch(e){body.innerHTML=`<div class="subscriber-empty"><h3>Não foi possível carregar a conta</h3><p>${esc(e.message)}</p><button id="subscriberRetry" type="button">Tentar novamente</button></div>`;body.querySelector('#subscriberRetry')?.addEventListener('click',render)}}
export function openSubscriberPortal(){render()}
export function initSubscriberPortal(){shell();window.addEventListener('routeflow:open-subscriber',render)}
