import{groupStops,optimizeOrder,flattenRoute}from'./core.js';
import{initAuthUI}from'./auth-ui.js';
import{syncImportedStops}from'./sync.js';
import{initDeliveryHistoryUI,recordCurrentDelivery}from'./delivery-history-ui.js';
import{initRegistryUI}from'./registry-ui.js';
import{initMobileUI}from'./mobile-ui.js';
import{initRouteHistoryUI}from'./route-history-ui.js';
import{beginRoute,markRouteProgress,finishRoute,clearActiveRoute,resumableRoute,syncActiveRoute}from'./route-session.js';
const $=id=>document.getElementById(id);let rows=[],headers=[],selectedHeaders=[],stops=[],matrix=[],order=[],filename='',map,markers=[],routeLine,optimizedRows=[],routeStats=null,currentLocation=null,dragFrom=null,navigationIndex=0,navMap=null,navLine=null,navMarker=null,navStopMarkers=[],navWatch=null,lastRerouteAt=0,lastNavPosition=null,resumeMode=false;const SERVICE_MINUTES=3,ARRIVAL_METERS=45,REROUTE_METERS=80,REROUTE_COOLDOWN=12000;
function setStatus(t){$('status').textContent=t}function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function fmtDuration(min){min=Math.max(0,Math.round(min));const h=Math.floor(min/60),m=min%60;return h?`${h}h ${String(m).padStart(2,'0')}min`:`${m} min`}function fmtClock(d){return d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}function distanceMeters(a,b){const R=6371000,p1=a.lat*Math.PI/180,p2=b.lat*Math.PI/180,dp=(b.lat-a.lat)*Math.PI/180,dl=(b.lon-a.lon)*Math.PI/180,x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.asin(Math.sqrt(x))}function resetStats(){$('routeKm').textContent=$('totalTime').textContent=$('finishTime').textContent=$('serviceTime').textContent='-';routeStats=null;$('startRoute').disabled=true;$('nextStopText').textContent='Otimize ou valide a rota para iniciar'}function phoneHeader(){return headers.find(h=>/(whats|telefone|celular|phone|fone|mobile)/i.test(h))||''}function sequenceHeader(){return headers.find(h=>/^sequence$/i.test(h.trim()))||headers.find(h=>/sequ[eê]ncia|sequence/i.test(h))||''}function importedSequences(stop){const h=sequenceHeader();if(!h||!stop)return[];return stop.rows.map(r=>String(r[h]??'').trim()).filter(v=>v!=='')}function sequenceText(stop){const values=importedSequences(stop);return values.length?`Sequence da planilha: ${values.join(', ')}`:'Sequence da planilha: não informado'}function normalizePhone(v){let n=String(v??'').replace(/\D/g,'');if((n.length===10||n.length===11)&&!n.startsWith('55'))n='55'+n;return n.length>=12&&n.length<=13?n:''}function whatsappUrl(row,stopNo){const phone=normalizePhone(row[phoneHeader()]);if(!phone)return'';return`https://wa.me/${phone}?text=${encodeURIComponent(`Olá! Sua entrega está prevista na rota de hoje. Você é a parada ${stopNo}. Endereço: ${row['Destination Address']||''}.`)}`}
function initMap(){if(map)return;map=L.map('map').setView([-23.55,-46.63],12);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(map)}function numberedIcon(n,count,active=false,done=false){return L.divIcon({className:`stop-wrap${active?' active':''}${done?' done':''}`,html:`<div class="stop-pin">${done?'✓':n}</div>${count>1?`<span class="stop-count">${count}</span>`:''}`,iconSize:[34,42],iconAnchor:[17,38]})}function drawPoints(){initMap();markers.forEach(m=>m.remove());markers=[];const seq=order.length?order:stops.map((_,i)=>i);seq.forEach((idx,n)=>markers.push(L.marker([stops[idx].lat,stops[idx].lon],{icon:numberedIcon(n+1,stops[idx].rows.length)}).bindTooltip(`<b>Parada ${n+1}</b><br>${esc(stops[idx].address)}`).addTo(map)));if(currentLocation)markers.push(L.circleMarker([currentLocation.lat,currentLocation.lon],{radius:9,weight:4}).bindTooltip('<b>Local atual / saída</b>').addTo(map));if(stops.length)map.fitBounds(L.latLngBounds([...stops.map(s=>[s.lat,s.lon]),...(currentLocation?[[currentLocation.lat,currentLocation.lon]]:[])]).pad(.12))}
function renderManual(){const seq=order.length?order:stops.map((_,i)=>i);$('manualList').innerHTML=seq.map((idx,n)=>`<div class="manual-stop" draggable="true" data-pos="${n}"><span class="drag">☰</span><b>${n+1}</b><div><strong>${esc(stops[idx].address)}</strong><small>${stops[idx].rows.length} entrega(s)</small></div><button class="move" data-dir="-1" data-pos="${n}">↑</button><button class="move" data-dir="1" data-pos="${n}">↓</button></div>`).join('');$('manualList').querySelectorAll('.manual-stop').forEach(el=>{el.addEventListener('dragstart',()=>dragFrom=Number(el.dataset.pos));el.addEventListener('dragover',e=>e.preventDefault());el.addEventListener('drop',e=>{e.preventDefault();moveStop(dragFrom,Number(el.dataset.pos))})});$('manualList').querySelectorAll('.move').forEach(b=>b.addEventListener('click',()=>moveStop(Number(b.dataset.pos),Number(b.dataset.pos)+Number(b.dataset.dir))));$('recalculate').disabled=!order.length}
function moveStop(from,to){const seq=order.length?[...order]:stops.map((_,i)=>i);if(from==null||to<0||to>=seq.length||from===to)return;const x=seq.splice(from,1)[0];seq.splice(to,0,x);order=seq;optimizedRows=flattenRoute(stops,order);resetStats();drawPoints();renderManual();renderColumns();$('export').disabled=true;$('notice').textContent='Ordem alterada. Recalcule antes de iniciar a navegação.'}
function renderColumns(){selectedHeaders=headers.filter((h,i)=>$(`col-${i}`)?.checked);const hs=selectedHeaders,seq=order.length?order:stops.map((_,i)=>i),out=[];$('thead').innerHTML='<th>Rota</th><th>Entrega</th><th>Contato</th>'+hs.map(h=>`<th>${esc(h)}</th>`).join('');seq.forEach((idx,n)=>stops[idx].rows.forEach((r,d)=>out.push({stopNo:n+1,d:d+1,count:stops[idx].rows.length,r})));$('tbody').innerHTML=out.map(x=>{const w=whatsappUrl(x.r,x.stopNo);return`<tr><td class="stopnum"><b>${x.stopNo}</b></td><td>${x.d}/${x.count}</td><td>${w?`<a class="wa" href="${w}" target="_blank" rel="noopener">WhatsApp</a>`:'-'}</td>${hs.map(h=>`<td>${esc(x.r[h]??'')}</td>`).join('')}</tr>`}).join('')}
function buildColumnPicker(){selectedHeaders=[...headers];$('columnOptions').innerHTML=headers.map((h,i)=>`<label><input type="checkbox" id="col-${i}" checked> ${esc(h)}</label>`).join('');$('columnOptions').querySelectorAll('input').forEach(x=>x.addEventListener('change',renderColumns))}
function fillStart(){const options=stops.map((s,i)=>`<option value="${i}">${i+1}. ${esc(s.address)}</option>`).join('');$('start').innerHTML=options}
async function readExcel(file){clearActiveRoute();resumeMode=false;setStatus('Lendo Excel…');filename=file.name;const wb=XLSX.read(await file.arrayBuffer(),{type:'array'}),ws=wb.Sheets[wb.SheetNames[0]];rows=XLSX.utils.sheet_to_json(ws,{defval:''});headers=rows.length?Object.keys(rows[0]):[];stops=groupStops(rows);order=[];optimizedRows=[];currentLocation=null;$('deliveries').textContent=rows.length;$('stops').textContent=stops.length;$('workspace').hidden=false;fillStart();buildColumnPicker();drawPoints();renderManual();renderColumns();resetStats();$('startRoute').textContent='▶ Iniciar rota';$('routeSummary').textContent=`${stops.length} paradas físicas para ${rows.length} entregas.`;setStatus('Cadastrando locais…');try{const sync=await syncImportedStops(stops);$('notice').textContent=sync.mode==='cloud'?`Planilha carregada. ${stops.length} locais sincronizados com sua conta. Escolha a saída e otimize a rota.`:`Planilha carregada. ${stops.length} locais cadastrados neste dispositivo. Escolha a saída e otimize a rota.`}catch(e){$('notice').textContent=`Planilha carregada e salva localmente. A sincronização com a nuvem ficou pendente: ${e.message}`}setStatus('Planilha pronta')}
async function fetchMatrix(){const coords=stops.map(s=>`${s.lon},${s.lat}`).join(';'),r=await fetch(`https://router.project-osrm.org/table/v1/driving/${coords}?annotations=distance,duration`);if(!r.ok)throw new Error('Falha ao consultar a malha viária');const d=await r.json();if(d.code!=='Ok'||!d.distances)throw new Error('Matriz viária inválida');return d}
async function optimize(){if(!stops.length)return;setStatus('Otimizando…');$('notice').textContent='Consultando sentidos permitidos e custos reais da malha viária…';try{const data=await fetchMatrix();matrix=data.distances;const start=Number($('start').value||0);order=optimizeOrder(stops,matrix,start,Number($('penalty').value||0));optimizedRows=flattenRoute(stops,order);await calculateRoute();renderManual();renderColumns();$('export').disabled=false;setStatus('Rota pronta')}catch(e){setStatus('Erro');$('notice').textContent=e.message}}
async function calculateRoute(){const seq=order.map(i=>stops[i]);const points=[...(currentLocation?[currentLocation]:[]),...seq].map(p=>`${p.lon},${p.lat}`).join(';'),r=await fetch(`https://router.project-osrm.org/route/v1/driving/${points}?overview=full&geometries=geojson&steps=false`),d=await r.json();if(d.code!=='Ok'||!d.routes?.[0])throw new Error('Não foi possível validar a rota dirigida.');const route=d.routes[0],service=seq.length*SERVICE_MINUTES,total=route.duration/60+service,finish=new Date(Date.now()+total*60000);routeStats={distance:route.distance,duration:route.duration,total,finish};$('routeKm').textContent=`${(route.distance/1000).toFixed(1)} km`;$('serviceTime').textContent=fmtDuration(service);$('totalTime').textContent=fmtDuration(total);$('finishTime').textContent=fmtClock(finish);$('routeSummary').textContent=`${seq.length} paradas · ${(route.distance/1000).toFixed(1)} km · ${fmtDuration(total)}`;$('notice').textContent='Rota validada pela malha viária. O tempo total inclui cerca de 3 min por parada.';$('startRoute').disabled=false;$('nextStopText').textContent=`Próxima: ${seq[0]?.address||'-'}`;if(routeLine)routeLine.remove();routeLine=L.geoJSON(route.geometry,{style:{weight:5,opacity:.75}}).addTo(map);drawPoints()}
function exportExcel(){if(!routeStats)return;const hs=selectedHeaders,out=optimizedRows.map(r=>{const o={'Optimized Sequence':r['Optimized Sequence'],'Optimized Stop':r['Optimized Stop']};for(const h of hs)o[h]=r[h]??'';return o}),wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(out),'Rota Otimizada');XLSX.writeFile(wb,`routeflow-otimizado-${filename.replace(/\.xlsx?$/i,'')}.xlsx`)}
function getPosition(){return new Promise((resolve,reject)=>{if(!navigator.geolocation)return reject(new Error('Geolocalização não disponível.'));navigator.geolocation.getCurrentPosition(p=>resolve({lat:p.coords.latitude,lon:p.coords.longitude,accuracy:p.coords.accuracy}),reject,{enableHighAccuracy:true,timeout:12000,maximumAge:3000})})}
async function useCurrentLocation(){setStatus('Obtendo GPS…');try{currentLocation=await getPosition();drawPoints();resetStats();$('notice').textContent=`Local atual definido como saída. Precisão aproximada: ${Math.round(currentLocation.accuracy)} m.`;setStatus('GPS pronto')}catch(e){setStatus('GPS indisponível');$('notice').textContent='Não foi possível obter o local atual. No celular, abra o RouteFlow por HTTPS e permita a localização.'}}
function initNavMap(){if(navMap)return;navMap=L.map('navMap',{zoomControl:false}).setView([-23.55,-46.63],15);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(navMap)}
function renderNavStops(){if(!navMap||!order.length)return;navStopMarkers.forEach(m=>m.remove());navStopMarkers=[];order.forEach((idx,n)=>{const s=stops[idx],marker=L.marker([s.lat,s.lon],{icon:numberedIcon(n+1,s.rows.length,n===navigationIndex,n<navigationIndex),zIndexOffset:n===navigationIndex?1200:500}).bindTooltip(`${n<navigationIndex?'Concluída':'Parada '+(n+1)}: ${esc(s.address)}`,{direction:'top'}).addTo(navMap);navStopMarkers.push(marker)})}
function updateNavigationUI(){if(!order.length)return;const s=stops[order[Math.min(navigationIndex,order.length-1)]];$('nextStopText').textContent=`Parada ${navigationIndex+1} de ${order.length}: ${s.address}`;if($('navSequence'))$('navSequence').textContent=`Parada ${navigationIndex+1} de ${order.length} · ${sequenceText(s)}`;$('nextRoute').disabled=navigationIndex>=order.length-1;if(navMap)renderNavStops()}
function speak(t){try{speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(t);u.lang='pt-BR';speechSynthesis.speak(u)}catch{}}
async function routeToCurrentStop(pos,force=false){if(!order.length)return;const dest=stops[order[navigationIndex]],now=Date.now();if(!force&&lastRerouteAt&&now-lastRerouteAt<REROUTE_COOLDOWN)return;const url=`https://router.project-osrm.org/route/v1/driving/${pos.lon},${pos.lat};${dest.lon},${dest.lat}?overview=full&geometries=geojson&steps=true`,r=await fetch(url),d=await r.json();if(d.code!=='Ok'||!d.routes?.[0])throw new Error('Sem rota até a próxima parada.');const rt=d.routes[0];lastRerouteAt=now;if(navLine)navLine.remove();navLine=L.geoJSON(rt.geometry,{style:{weight:7,opacity:.85}}).addTo(navMap);$('navDistance').textContent=rt.distance>=1000?`${(rt.distance/1000).toFixed(1)} km`:`${Math.round(rt.distance)} m`;$('navEta').textContent=Math.max(1,Math.round(rt.duration/60));$('navArrival').textContent=fmtClock(new Date(Date.now()+rt.duration*1000));$('navDestination').textContent=`${dest.address} · ${dest.rows.length} entrega(s)`;if($('navSequence'))$('navSequence').textContent=`Parada ${navigationIndex+1} de ${order.length} · ${sequenceText(dest)}`;const steps=rt.legs?.[0]?.steps||[],next=steps.find(s=>s.distance>20)||steps[0];let instruction='Siga pela rota indicada';if(next){const name=next.name||'';instruction=next.maneuver?.type==='turn'?`Vire ${name}`:`Siga ${name}`.trim()}$('navInstruction').textContent=instruction;$('navInstructionDistance').textContent=next?.distance?`em ${Math.round(next.distance)} m`:'';renderNavStops();navMap.fitBounds(L.geoJSON(rt.geometry).getBounds().pad(.18));if(force)speak(`${instruction}. Próxima parada: ${dest.address}`)}
function operationalSnapshot(){return{rows,headers,selectedHeaders,stops,order,filename,routeStats:routeStats?{distance:routeStats.distance,duration:routeStats.duration,total:routeStats.total}:null,currentLocation}}
async function openNavigator(){ $('navigator').hidden=false;document.body.style.overflow='hidden';initNavMap();setTimeout(()=>navMap.invalidateSize(),80);updateNavigationUI();renderNavStops();$('navInstruction').textContent='Obtendo sua localização…';$('navInstructionDistance').textContent='';try{const p=await getPosition();lastNavPosition=p;window.routeflowLastPosition=p;await updateNavPosition(p,true)}catch(e){$('navInstruction').textContent='GPS indisponível';$('navInstructionDistance').textContent='Permita a localização no navegador para continuar.';return}if(navWatch)navigator.geolocation.clearWatch(navWatch);navWatch=navigator.geolocation.watchPosition(p=>updateNavPosition({lat:p.coords.latitude,lon:p.coords.longitude,accuracy:p.coords.accuracy}).catch(()=>{}),()=>{}, {enableHighAccuracy:true,maximumAge:2000,timeout:10000})}
async function startNativeNavigation(){if(resumeMode){await openNavigator();return}if(!routeStats||!order.length)return;navigationIndex=0;await beginRoute({sourceFilename:filename,plannedKm:routeStats.distance/1000,plannedMinutes:Math.round(routeStats.total),stops:order.length,deliveries:rows.length,operational:operationalSnapshot()});await openNavigator()}
async function updateNavPosition(pos,force=false){lastNavPosition=pos;window.routeflowLastPosition=pos;$('navAccuracy').textContent=`${Math.round(pos.accuracy||0)} m`;if(navMarker)navMarker.remove();navMarker=L.circleMarker([pos.lat,pos.lon],{radius:9,weight:5}).addTo(navMap);const dest=stops[order[navigationIndex]],dist=distanceMeters(pos,dest);if(dist<=ARRIVAL_METERS){$('navInstruction').textContent='Você chegou à parada';$('navInstructionDistance').textContent=`${Math.round(dist)} m do destino`;speak(`Você chegou à parada ${navigationIndex+1}. ${dest.address}`);return}await routeToCurrentStop(pos,force||!navLine)}
async function completeCurrentStop(){recordCurrentDelivery();const completed=navigationIndex+1;await markRouteProgress(completed);if(navigationIndex>=order.length-1){await finishRoute();resumeMode=false;speak('Rota concluída.');$('notice').textContent='Rota concluída. Todas as paradas foram atendidas.';stopNativeNavigation();return}navigationIndex++;updateNavigationUI();$('navInstruction').textContent='Calculando próxima parada…';if(lastNavPosition){try{await routeToCurrentStop(lastNavPosition,true)}catch{$('navInstruction').textContent='Não foi possível calcular a próxima parada.'}}}
function stopNativeNavigation(){if(navWatch){navigator.geolocation.clearWatch(navWatch);navWatch=null}$('navigator').hidden=true;document.body.style.overflow='';$('nextRoute').disabled=false}
function restoreInterruptedRoute(){const route=resumableRoute(),op=route?.operational;if(!route)return;if(!op?.stops?.length||!op?.order?.length){setStatus('Rota antiga interrompida');$('workspace').hidden=false;$('notice').textContent=`A rota ${route.sourceFilename||''} foi criada antes da retomada automática. Importe a planilha uma última vez para iniciar uma nova rota recuperável.`;$('nextStopText').textContent=`Rota antiga · ${route.completedStops||0}/${route.stops||0} paradas`;return}rows=op.rows||[];headers=op.headers||[];selectedHeaders=op.selectedHeaders||headers;stops=op.stops;order=op.order;filename=op.filename||route.sourceFilename||'';currentLocation=op.currentLocation||null;routeStats=op.routeStats?{...op.routeStats,finish:new Date(Date.now()+Number(op.routeStats.total||0)*60000)}:null;navigationIndex=Math.min(Number(route.completedStops||0),order.length-1);resumeMode=true;$('workspace').hidden=false;$('deliveries').textContent=rows.length||route.deliveries||0;$('stops').textContent=stops.length||route.stops||0;fillStart();buildColumnPicker();drawPoints();renderManual();renderColumns();if(routeStats){$('routeKm').textContent=`${(routeStats.distance/1000).toFixed(1)} km`;$('serviceTime').textContent=fmtDuration(stops.length*SERVICE_MINUTES);$('totalTime').textContent=fmtDuration(routeStats.total);$('finishTime').textContent=fmtClock(routeStats.finish)}$('routeSummary').textContent=`Rota recuperada · ${route.completedStops||0}/${order.length} paradas concluídas.`;$('startRoute').disabled=false;$('startRoute').textContent='▶ Continuar rota';updateNavigationUI();setStatus('Rota recuperada');$('notice').textContent=`Rota recuperada automaticamente. Próximo destino: parada ${navigationIndex+1} de ${order.length}.`}
$('file').addEventListener('change',e=>e.target.files[0]&&readExcel(e.target.files[0]));$('optimize').addEventListener('click',optimize);$('recalculate').addEventListener('click',async()=>{setStatus('Recalculando…');try{await calculateRoute();optimizedRows=flattenRoute(stops,order);renderColumns();$('export').disabled=false;setStatus('Rota pronta')}catch(e){setStatus('Erro');$('notice').textContent=e.message}});$('export').addEventListener('click',exportExcel);$('currentLocation').addEventListener('click',useCurrentLocation);$('allColumns').addEventListener('click',()=>{$('columnOptions').querySelectorAll('input').forEach(x=>x.checked=true);renderColumns()});$('noColumns').addEventListener('click',()=>{$('columnOptions').querySelectorAll('input').forEach(x=>x.checked=false);renderColumns()});$('startRoute').addEventListener('click',startNativeNavigation);$('stopNavigation').addEventListener('click',stopNativeNavigation);$('completeStop').addEventListener('click',completeCurrentStop);$('nextRoute').addEventListener('click',completeCurrentStop);
window.addEventListener('routeflow:active-route-synced',e=>{if(e.detail?.status==='ACTIVE'&&e.detail?.operational?.stops?.length&&!resumeMode)restoreInterruptedRoute()});
initAuthUI();initDeliveryHistoryUI();initRegistryUI();initRouteHistoryUI();initMobileUI();
initMap();resetStats();restoreInterruptedRoute();syncActiveRoute().then(route=>{if(route?.status==='ACTIVE'&&route?.operational?.stops?.length)restoreInterruptedRoute()}).catch(()=>{});